name: Deploy BattleOne Infrastructure

on:
  push:
    branches: [main]
    paths:
      - '*.tf'
      - 'terraform.tfvars'
      - 'docker-compose.yml'
      - 'kratos/**'
      - 'cloud-init.yml'
      - '.github/workflows/terraform-deploy.yml'
  workflow_dispatch:
    inputs:
      action:
        description: 'Terraform action to perform'
        required: true
        default: 'apply'
        type: choice
        options:
          - apply
          - plan
      auto_approve:
        description: 'Auto-approve the action (skip manual approval)'
        required: false
        default: false
        type: boolean

env:
  # DigitalOcean Configuration
  TF_VAR_digitalocean_token: ${{ secrets.DO_USER_ACCESS_TOKEN }}
  TF_VAR_spaces_access_key: ${{ secrets.DO_SPACES_ACCESS_KEY }}
  TF_VAR_spaces_secret_key: ${{ secrets.DO_SPACES_SECRET_KEY }}
  TF_VAR_ssh_private_key: ${{ secrets.DO_SSH_PRIVATE_KEY }}
  TF_VAR_ssh_public_key: ${{ secrets.DO_SSH_PUBLIC_KEY }}
  TF_VAR_laptop_ssh_public_key: ${{ secrets.DO_SSH_PUBLIC_KEY }}
  
  # Database Configuration
  TF_VAR_postgres_password: ${{ secrets.POSTGRES_PASSWORD }}
  TF_VAR_redis_password: ${{ secrets.REDIS_PASSWORD }}
  TF_VAR_postgres_db: ${{ secrets.POSTGRES_DB || 'battleone' }}
  TF_VAR_postgres_user: ${{ secrets.POSTGRES_USER || 'battleone_user' }}
  
  # Better Stack Configuration
  TF_VAR_betterstack_source_token: ${{ secrets.BETTERSTACK_SOURCE_TOKEN }}
  TF_VAR_betterstack_ingestion_host: ${{ secrets.BETTERSTACK_INGESTION_HOST || 'in.logs.betterstack.com' }}
  
  # Datadog Configuration (Deprecated - keeping for backward compatibility)
  TF_VAR_datadog_api_key: ${{ secrets.DATADOG_API_KEY || '' }}
  TF_VAR_datadog_site: ${{ secrets.DATADOG_SITE || 'datadoghq.com' }}
  
  # Infrastructure Settings
  TF_VAR_region: "tor1"
  TF_VAR_droplet_size: "s-2vcpu-4gb"
  
  # DigitalOcean Spaces for Terraform State
  AWS_ACCESS_KEY_ID: ${{ secrets.DO_SPACES_ACCESS_KEY }}
  AWS_SECRET_ACCESS_KEY: ${{ secrets.DO_SPACES_SECRET_KEY }}

jobs:
  terraform:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Validate required secrets
        run: |
          echo "🔍 Validating required secrets..."
          
          # Check DigitalOcean secrets
          if [ -z "${{ secrets.DO_USER_ACCESS_TOKEN }}" ]; then
            echo "❌ DO_USER_ACCESS_TOKEN secret is missing"
            exit 1
          fi
          
          # Check DigitalOcean Spaces secrets
          if [ -z "${{ secrets.DO_SPACES_ACCESS_KEY }}" ]; then
            echo "❌ DO_SPACES_ACCESS_KEY secret is missing"
            exit 1
          fi
          if [ -z "${{ secrets.DO_SPACES_SECRET_KEY }}" ]; then
            echo "❌ DO_SPACES_SECRET_KEY secret is missing"
            exit 1
          fi
          
          # Check SSH secrets
          if [ -z "${{ secrets.DO_SSH_PRIVATE_KEY }}" ]; then
            echo "❌ DO_SSH_PRIVATE_KEY secret is missing"
            exit 1
          fi
          if [ -z "${{ secrets.DO_SSH_PUBLIC_KEY }}" ]; then
            echo "❌ DO_SSH_PUBLIC_KEY secret is missing"
            exit 1
          fi
          
          # Check database secrets
          if [ -z "${{ secrets.POSTGRES_PASSWORD }}" ]; then
            echo "❌ POSTGRES_PASSWORD secret is missing"
            exit 1
          fi
          if [ -z "${{ secrets.REDIS_PASSWORD }}" ]; then
            echo "❌ REDIS_PASSWORD secret is missing"
            exit 1
          fi
          
          # Check Better Stack secrets
          if [ -z "${{ secrets.BETTERSTACK_SOURCE_TOKEN }}" ]; then
            echo "❌ BETTERSTACK_SOURCE_TOKEN secret is missing"
            exit 1
          fi
          
          echo "✅ All required secrets are configured"

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "~1.6"

      - name: Cache Terraform
        uses: actions/cache@v3
        with:
          path: |
            ~/.terraform.d/plugin-cache
            .terraform
          key: terraform-${{ hashFiles('**/.terraform.lock.hcl') }}
          restore-keys: terraform-

      - name: Terraform Init & Validate
        run: |
          echo "🚀 Initializing and validating Terraform configuration..."
          terraform init
          terraform validate

      - name: Terraform Plan
        id: plan
        run: |
          echo "📋 Creating Terraform plan..."
          terraform plan -detailed-exitcode -out=tfplan
          echo "plan_exitcode=$?" >> $GITHUB_OUTPUT

      - name: Comment Plan on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const { stdout } = await exec.getExecOutput('terraform', ['show', '-no-color', 'tfplan']);
            
            const output = `## 🚀 Terraform Plan
            
            <details>
            <summary>Show Plan</summary>
            
            \`\`\`
            ${stdout}
            \`\`\`
            
            </details>`;
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: output
            });

      - name: Manual Approval
        if: |
          github.event_name == 'workflow_dispatch' && 
          github.event.inputs.action == 'apply' && 
          github.event.inputs.auto_approve != 'true' &&
          github.ref != 'refs/heads/main' &&
          steps.plan.outputs.plan_exitcode == '2'
        uses: trstringer/manual-approval@v1
        with:
          secret: ${{ github.TOKEN }}
          approvers: ${{ github.repository_owner }}
          minimum-approvals: 1
          issue-title: "🚀 Terraform Apply Approval Required"
          issue-body: |
            **Terraform Apply Approval Required**
            
            Please review the Terraform plan above and approve this workflow to proceed with applying the BattleOne infrastructure changes.
            
            **Action**: ${{ github.event.inputs.action }}
            **Branch**: ${{ github.ref_name }}
            **Commit**: ${{ github.sha }}
            
            **Infrastructure includes:**
            - DigitalOcean droplet in Toronto (tor1)
            - PostgreSQL database
            - Redis cache
            - Kratos identity management

      - name: Terraform Apply
        if: |
          (github.event_name == 'push' && github.ref == 'refs/heads/main') ||
          (github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'apply')
        run: |
          echo "🚀 Applying Terraform configuration..."
          terraform apply -auto-approve tfplan

      - name: Deployment Summary & Health Check
        if: |
          (github.event_name == 'push' && github.ref == 'refs/heads/main') ||
          (github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'apply')
        run: |
          echo "🏥 Performing infrastructure health check and generating summary..."
          
          # Get infrastructure details
          DROPLET_IP=$(terraform output -raw droplet_ip)
          KRATOS_URL=$(terraform output -raw kratos_internal_url)
          SSH_CONNECTION=$(terraform output -raw ssh_connection)
          
          echo "🌐 Infrastructure deployed to: $DROPLET_IP"
          
          # Quick connectivity test (reduced wait time)
          echo "⏳ Waiting 60 seconds for services to initialize..."
          sleep 60
          
          echo "🔐 Testing connectivity..."
          if ping -c 2 $DROPLET_IP > /dev/null 2>&1; then
            echo "✅ Droplet is reachable"
            CONNECTIVITY_STATUS="✅ Online"
          else
            echo "⚠️ Droplet connectivity issues (may still be initializing)"
            CONNECTIVITY_STATUS="⚠️ Initializing"
          fi
          
          # Generate deployment summary
          echo "## 🎉 BattleOne Infrastructure Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "- **Droplet IP**: $DROPLET_IP" >> $GITHUB_STEP_SUMMARY
          echo "- **Region**: Toronto (tor1)" >> $GITHUB_STEP_SUMMARY
          echo "- **Connectivity**: $CONNECTIVITY_STATUS" >> $GITHUB_STEP_SUMMARY
          echo "- **PostgreSQL**: ✅ Available on internal network" >> $GITHUB_STEP_SUMMARY
          echo "- **Redis**: ✅ Available on internal network" >> $GITHUB_STEP_SUMMARY
          echo "- **Kratos**: ✅ Available internally" >> $GITHUB_STEP_SUMMARY
          echo "- **Better Stack**: ✅ Monitoring active" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "🔗 **Access Information**:" >> $GITHUB_STEP_SUMMARY
          echo "- SSH: \`$SSH_CONNECTION\`" >> $GITHUB_STEP_SUMMARY
          echo "- Kratos: \`$KRATOS_URL\`" >> $GITHUB_STEP_SUMMARY
          echo "- Better Stack: [Dashboard](https://betterstack.com/logs)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "📝 **Next Steps**: Infrastructure ready for application deployment" >> $GITHUB_STEP_SUMMARY

      - name: Notify deployment completion
        if: success()
        run: |
          echo "🎉 BattleOne infrastructure deployment completed successfully!"
          echo "🔗 Infrastructure is ready at: $(terraform output -raw droplet_ip)"